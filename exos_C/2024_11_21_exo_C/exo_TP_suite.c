#include "lib.h"

int main() {
}
Map* get_map(int height, int width) {
	Map* map = malloc(sizeof(Map));
	// logique height/width
	return map;
}

void load_blocks(Map* map) {
		//for X in range height
		//save Block blocks_top[width]
		//	for y in range width
		// save current_block
		//Block current_block {
			// top[width],
			//0,
			//0,
			//block_left
			//};
			// current_block.top.bot = current_block;
			// current_block.meft.right = current_block;
			//update last_row
