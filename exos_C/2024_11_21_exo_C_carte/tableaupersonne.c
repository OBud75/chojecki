#include <stdio.h>

int main(void) {
	struct Person people[3];
	struct Person *add_p = people;

	for (int i = 1; i <3; i++) {
		add_p1->id = i ;
		add_p += 1;


	}
	for (int y=1; y<2; y++) {
		struct Person p = people[y];
	}
}
